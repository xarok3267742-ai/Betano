# Release Artifacts

`app-release-signed.aab` is the signed Android App Bundle handoff for Google Play upload testing.

Signing credentials are stored outside this repository and must never be committed.
